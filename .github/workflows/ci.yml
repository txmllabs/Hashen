name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Secret scanning (gitleaks)
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install (single)
        run: python -m pip install -U pip && pip install -e ".[dev]"

      - name: Compile check
        run: python -m compileall -q src

      - name: Pip check
        run: pip check

      - name: Pytest
        run: pytest -q

      - name: Ruff check
        run: ruff check src tests

      - name: Ruff format check
        run: ruff format --check src tests

      - name: pip-audit
        run: pip-audit --strict --desc

      - name: CycloneDX SBOM
        run: |
          mkdir -p sbom
          cyclonedx-py environment --pyproject pyproject.toml --outfile sbom/bom.json --output-format JSON

      - name: Evidence-bundle smoke test
        run: |
          echo "hashen-ci" > sample.bin
          python tools/run_evidence_bundle.py sample.bin ci-run --output-dir bundle_ci
          python tools/verify_bundle.py bundle_ci

      - name: Benchmark smoke test
        run: |
          hashen benchmark run --domain audio --samples 5 --seed 42 --output /tmp/bench.json
          python -c "import json; d=json.load(open('/tmp/bench.json')); assert 'auc' in d"

      - name: API smoke test
        run: |
          pip install -e ".[api]"
          python -c "
          from fastapi.testclient import TestClient
          from hashen.api.app import app
          c = TestClient(app)
          assert c.get('/api/v1/health').status_code == 200
          "

      - name: Upload SBOM
        uses: actions/upload-artifact@v4
        with:
          name: sbom
          path: sbom/
          retention-days: 7
